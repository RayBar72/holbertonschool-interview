#include "search.h"

int advanced_binary(int *array, size_t size, int value)
{
	int *recorre = array, step = 0;
	size_t counter = 0;

	if (!array)
		return (-1);

	step = counter / 2;

	while (step)
	{
		/* code */
	}
	
}